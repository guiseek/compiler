{
  "name": "@guiseek/compiler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": {
    "dev": "./bin/run.js"
  },
  "scripts": {
    "build": "tsc && cp README.md dist/",
    "format": "npx prettier --write 'src/**/*.{ts,js,json,md}'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.5.2"
  },
  "devDependencies": {
    "chalk": "^4.1.2"
  }
}
